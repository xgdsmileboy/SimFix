package cofix.core.modify.pattern;

import cofix.core.parser.node.Node.TYPE;

public class RevPattern extends Pattern {

	protected RevPattern(TYPE srcType, TYPE tarType) {
		super(srcType, tarType);
	}

}
